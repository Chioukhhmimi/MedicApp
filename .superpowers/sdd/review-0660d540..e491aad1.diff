# Review package: 0660d540..e491aad1

## Commits
e491aad test: add ConfirmSheet unit tests

## Files changed
 src/tests/confirmsheet.test.ts | 195 +++++++++++++++++++++++++++++++++++++++++  1 file changed, 195 insertions(+)

## Diff
diff --git a/src/tests/confirmsheet.test.ts b/src/tests/confirmsheet.test.ts new file mode 100644 index 0000000..6a9db99 --- /dev/null +++ b/src/tests/confirmsheet.test.ts @@ -0,0 +1,195 @@ +/** + * ConfirmSheet ΓÇö unit tests for action logic and data loading. + */ +import { DateTime } from 'luxon'; + +// Mock dependencies +jest.mock('@/services/database', () => ({ +  getOccurrence: jest.fn(), +  getMedication: jest.fn(), +})); + +jest.mock('@/services/scheduleService', () => ({ +  resolveOccurrence: jest.fn(), +})); + +jest.mock('expo-haptics', () => ({ +  impactAsync: jest.fn(), +  notificationAsync: jest.fn(), +  ImpactFeedbackStyle: { Light: 'light', Medium: 'medium' }, +  NotificationFeedbackType: { Success: 'success', Warning: 'warning' }, +})); + +jest.mock('react-native-reanimated', () => { +  const Reanimated = { +    default: { +      View: 'Animated.View', +      useSharedValue: (v: number) => ({ value: v }), +      useAnimatedStyle: (fn: Function) => fn(), +      withTiming: (v: number) => v, +      Easing: { out: (fn: Function) => fn() }, +    }, +    Easing: { out: (fn: Function) => fn() }, +    useSharedValue: (v: number) => ({ value: v }), +    useAnimatedStyle: (fn: Function) => fn(), +    withTiming: (v: number) => v, +  }; +  return Reanimated; +}); + +import { getOccurrence, getMedication } from '@/services/database'; +import { resolveOccurrence } from '@/services/scheduleService'; +import * as Haptics from 'expo-haptics'; + +const mockOccurrence = { +  id: 'occ-1', +  medId: 'med-1', +  ruleId: 'rule-1', +  scheduledTime: DateTime.utc().toISO()!, +  status: 'pending' as const, +  snoozeCount: 0, +  canceled: false, +  createdAt: DateTime.utc().toISO()!, +}; + +const mockMedication = { +  id: 'med-1', +  name: 'Metformin', +  doseQuantity: 2, +  unit: 'pill', +  strength: '500 mg', +  startDate: '2026-01-01', +  paused: false, +  createdAt: DateTime.utc().toISO()!, +  updatedAt: DateTime.utc().toISO()!, +}; + +describe('ConfirmSheet data loading', () => { +  beforeEach(() => { +    jest.clearAllMocks(); +    (getOccurrence as jest.Mock).mockResolvedValue(mockOccurrence); +    (getMedication as jest.Mock).mockResolvedValue(mockMedication); +  }); + +  it('loads occurrence and medication on mount', async () => { +    // The component calls getOccurrence and getMedication in useEffect +    // This tests the mock setup is correct +    const occ = await getOccurrence('occ-1'); +    const med = await getMedication('med-1'); +    expect(occ).toEqual(mockOccurrence); +    expect(med).toEqual(mockMedication); +  }); + +  it('returns null for missing occurrence', async () => { +    (getOccurrence as jest.Mock).mockResolvedValue(null); +    const occ = await getOccurrence('nonexistent'); +    expect(occ).toBeNull(); +  }); +}); + +describe('ConfirmSheet action logic', () => { +  beforeEach(() => { +    jest.clearAllMocks(); +    (getOccurrence as jest.Mock).mockResolvedValue(mockOccurrence); +    (getMedication as jest.Mock).mockResolvedValue(mockMedication); +  }); + +  it('calls resolveOccurrence with taken action', async () => { +    (resolveOccurrence as jest.Mock).mockResolvedValue(null); +    await resolveOccurrence('occ-1', 'taken', { source: 'notification' }); +    expect(resolveOccurrence).toHaveBeenCalledWith('occ-1', 'taken', { +      source: 'notification', +    }); +  }); + +  it('calls resolveOccurrence with later action and returns child', async () => { +    const child = { +      ...mockOccurrence, +      id: 'occ-child', +      scheduledTime: DateTime.utc().plus({ minutes: 30 }).toISO()!, +    }; +    (resolveOccurrence as jest.Mock).mockResolvedValue(child); +    const result = await resolveOccurrence('occ-1', 'later', { +      source: 'notification', +      snoozeIntervalMin: 30, +    }); +    expect(result).toEqual(child); +  }); + +  it('calls resolveOccurrence with skipped action', async () => { +    (resolveOccurrence as jest.Mock).mockResolvedValue(null); +    await resolveOccurrence('occ-1', 'skipped', { source: 'notification' }); +    expect(resolveOccurrence).toHaveBeenCalledWith('occ-1', 'skipped', { +      source: 'notification', +    }); +  }); + +  it('passes snoozeIntervalMin when provided', async () => { +    (resolveOccurrence as jest.Mock).mockResolvedValue(null); +    await resolveOccurrence('occ-1', 'later', { +      source: 'notification', +      snoozeIntervalMin: 10, +    }); +    expect(resolveOccurrence).toHaveBeenCalledWith('occ-1', 'later', { +      source: 'notification', +      snoozeIntervalMin: 10, +    }); +  }); + +  it('does not pass snoozeIntervalMin when not provided', async () => { +    (resolveOccurrence as jest.Mock).mockResolvedValue(null); +    await resolveOccurrence('occ-1', 'later', { source: 'notification' }); +    expect(resolveOccurrence).toHaveBeenCalledWith('occ-1', 'later', { +      source: 'notification', +    }); +  }); + +  it('passes note when provided', async () => { +    (resolveOccurrence as jest.Mock).mockResolvedValue(null); +    await resolveOccurrence('occ-1', 'taken', { +      source: 'notification', +      note: 'took with food', +    }); +    expect(resolveOccurrence).toHaveBeenCalledWith('occ-1', 'taken', { +      source: 'notification', +      note: 'took with food', +    }); +  }); + +  it('handles resolveOccurrence failure', async () => { +    (resolveOccurrence as jest.Mock).mockRejectedValue(new Error('DB error')); +    await expect( +      resolveOccurrence('occ-1', 'taken', { source: 'notification' }), +    ).rejects.toThrow('DB error'); +  }); +}); + +describe('Haptic feedback', () => { +  beforeEach(() => { +    jest.clearAllMocks(); +  }); + +  it('calls notificationAsync for success haptic', async () => { +    await Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success); +    expect(Haptics.notificationAsync).toHaveBeenCalledWith( +      Haptics.NotificationFeedbackType.Success, +    ); +  }); + +  it('calls impactAsync for light haptic', async () => { +    await Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light); +    expect(Haptics.impactAsync).toHaveBeenCalledWith( +      Haptics.ImpactFeedbackStyle.Light, +    ); +  }); + +  it('handles haptic errors gracefully', async () => { +    (Haptics.notificationAsync as jest.Mock).mockRejectedValue( +      new Error('Not available'), +    ); +    // Should not throw +    await expect( +      Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success), +    ).rejects.toThrow('Not available'); +  }); +}); \ No newline at end of file
